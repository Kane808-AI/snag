# Secrets / credentials — NEVER commit
.env

# Runtime data (personal vault + backups) — NEVER commit
data/
logs/
.linear_bridge_state.json

# Dashboard build artifacts (exported vault content) — NEVER commit
dashboard/data.json
dashboard/last_built.json

# Strategy / knowledge base — private, not public
kb/

# Python
__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/

# Node (web PWA)
web/node_modules/
web/.next/
web/out/
*.tsbuildinfo

# Research / backups (not app code, kept local only)
blueprint-teardown/
web-nextjs-backup/

# OS / editor
.DS_Store
